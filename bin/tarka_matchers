#!/usr/bin/env ruby
def install argv
	require 'tarka_matchers/commands/install'
	TarkaMatchers::Commands::Install.install argv.drop(1)
end
case ARGV[0]
when :install
	install ARGV
else
	install ARGV
end
